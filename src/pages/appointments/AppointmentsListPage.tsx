import { Outlet } from 'react-router-dom';

const AppointmentsListPage = () => {
  return (
    <>
      <div>AppointmentsListPage</div>
      <Outlet />
    </>
  );
};

export default AppointmentsListPage;
