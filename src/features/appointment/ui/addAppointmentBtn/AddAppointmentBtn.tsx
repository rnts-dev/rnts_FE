import './addAppointmentBtn.scss';

const AddAppointmentBtn = () => {
  return (
    <button className="add_appointment_btn" onClick={() => {}}>
      약속 등록하기
    </button>
  );
};

export default AddAppointmentBtn;
